<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>Filesystem &amp; I/O</title>
<date>2025-11-15</date>
<copyright>
<holder>zigbook</holder>
</copyright>
</info>
<chapter xml:id="overview">
<title>Overview</title>
<simpara>Workspace builds are only as useful as the data they sling around. After wiring a multi-package dashboard in Chapter 27, we now descend into the filesystem and I/O primitives that back every package install, log collector, and CLI tool you will write. See <link xl:href="27__project-multi-package-workspace-and-vendor.xml">27</link>. Zig <link xl:href="https://ziglang.org/download/0.15.1/release-notes.html-">v0.15.2</link> brings a unified <literal>std.fs.File</literal> surface with memoized metadata and a buffered-writer story that the changelog all but shouts about—use it, flush it, and keep handles tidy. See <link xl:href="https://github.com/ziglang/zig/tree/master/lib/std/fs/File.zig">File.zig</link>.</simpara>
<section xml:id="_the_filesystem_architecture">
<title>The Filesystem Architecture</title>
<simpara>Before diving into specific operations, it&#8217;s essential to understand how Zig&#8217;s filesystem APIs are structured. The following diagram shows the layered architecture from high-level <literal>std.fs</literal> operations down to system calls:</simpara>
<literallayout class="monospaced">graph TB
    subgraph "User Code"
        APP[Application Code]
    end

    subgraph "High-Level APIs (lib/std)"
        FS["std.fs&lt;br/&gt;(fs.zig)"]
        NET["std.net&lt;br/&gt;(net.zig)"]
        PROCESS["std.process&lt;br/&gt;(process.zig)"]
        FMT["std.fmt&lt;br/&gt;(fmt.zig)"]
        HEAP["std.heap&lt;br/&gt;(heap.zig)"]
    end

    subgraph "Mid-Level Abstractions"
        POSIX["std.posix&lt;br/&gt;(posix.zig)&lt;br/&gt;Cross-platform POSIX API"]
        OS["std.os&lt;br/&gt;(os.zig)&lt;br/&gt;OS-specific wrappers"]
        MEM["std.mem&lt;br/&gt;(mem.zig)&lt;br/&gt;Memory utilities"]
        DEBUG["std.debug&lt;br/&gt;(debug.zig)&lt;br/&gt;Stack traces, assertions"]
    end

    subgraph "Platform Layer"
        LINUX["std.os.linux&lt;br/&gt;(os/linux.zig)&lt;br/&gt;Direct syscalls"]
        WINDOWS["std.os.windows&lt;br/&gt;(os/windows.zig)&lt;br/&gt;Win32 APIs"]
        WASI["std.os.wasi&lt;br/&gt;(os/wasi.zig)&lt;br/&gt;WASI APIs"]
        LIBC["std.c&lt;br/&gt;(c.zig)&lt;br/&gt;C interop"]
    end

    subgraph "System Layer"
        SYSCALL["System Calls"]
        KERNEL["Operating System"]
    end

    APP --&gt; FS
    APP --&gt; NET
    APP --&gt; PROCESS
    APP --&gt; FMT
    APP --&gt; HEAP

    FS --&gt; POSIX
    NET --&gt; POSIX
    PROCESS --&gt; POSIX
    FMT --&gt; MEM
    HEAP --&gt; MEM

    POSIX --&gt; OS
    OS --&gt; LIBC
    OS --&gt; LINUX
    OS --&gt; WINDOWS
    OS --&gt; WASI

    DEBUG --&gt; OS

    LINUX --&gt; SYSCALL
    WINDOWS --&gt; SYSCALL
    WASI --&gt; SYSCALL
    LIBC --&gt; SYSCALL

    SYSCALL --&gt; KERNEL</literallayout>
<simpara>This layered design provides both portability and control. When you call <literal>std.fs.File.read()</literal>, the request flows through <literal>std.posix</literal> for cross-platform compatibility, then through <literal>std.os</literal> which dispatches to platform-specific implementations—either direct system calls on Linux or libc functions when <literal>builtin.link_libc</literal> is true. Understanding this architecture helps you reason about cross-platform behavior, debug issues by knowing which layer to inspect, and make informed decisions about linking libc. The separation of concerns means you can use high-level <literal>std.fs</literal> APIs for portability while still having access to lower layers when you need platform-specific features.</simpara>
</section>
</chapter>
<chapter xml:id="learning-goals">
<title>Learning Goals</title>
<itemizedlist>
<listitem>
<simpara>Compose platform-neutral paths, open files safely, and print via buffered writers without leaking handles. <link xl:href="https://github.com/ziglang/zig/tree/master/lib/std/fs/path.zig">path.zig</link></simpara>
</listitem>
<listitem>
<simpara>Stream data between files while inspecting metadata such as byte counts and stat output.</simpara>
</listitem>
<listitem>
<simpara>Walk directory trees using <literal>Dir.walk</literal>, filtering on extensions to build discovery and housekeeping tools. <link xl:href="https://github.com/ziglang/zig/tree/master/lib/std/fs/Dir.zig">Dir.zig</link></simpara>
</listitem>
<listitem>
<simpara>Apply ergonomic error handling patterns (<literal>catch</literal>, cleanup defers) when juggling multiple file descriptors.</simpara>
</listitem>
</itemizedlist>
</chapter>
<chapter xml:id="paths-and-writers">
<title>Paths, handles, and buffered stdout</title>
<simpara>We start with the basics: join a platform-neutral path, create a file, write a CSV header with the buffered stdout guidance from 0.15, and read it back into memory. The example keeps allocations explicit so you can see where buffers live and when they are freed.</simpara>
<section xml:id="_understanding_std_fs_module_organization">
<title>Understanding std.fs Module Organization</title>
<simpara>The <literal>std.fs</literal> namespace is organized around two primary types, each with distinct responsibilities:</simpara>
<literallayout class="monospaced">graph TB
    subgraph "std.fs Module"
        FS["fs.zig&lt;br/&gt;cwd, max_path_bytes"]
        DIR["fs/Dir.zig&lt;br/&gt;openFile, makeDir"]
        FILE["fs/File.zig&lt;br/&gt;read, write, stat"]
    end

    FS --&gt; DIR
    FS --&gt; FILE</literallayout>
<simpara>The <literal>fs.zig</literal> root module provides entry points like <literal>std.fs.cwd()</literal> which returns a <literal>Dir</literal> handle representing the current working directory, plus platform constants like <literal>max_path_bytes</literal>. The <literal>Dir</literal> type (<literal>fs/Dir.zig</literal>) handles directory-level operations—opening files, creating subdirectories, iterating entries, and managing directory handles. The <literal>File</literal> type (<literal>fs/File.zig</literal>) provides all file-specific operations: reading, writing, seeking, and querying metadata via <literal>stat()</literal>. This separation keeps the API clear: use <literal>Dir</literal> methods to navigate the filesystem tree and <literal>File</literal> methods to manipulate file contents. When you call <literal>dir.openFile()</literal>, you get back a <literal>File</literal> handle that&#8217;s independent of the directory—closing the directory doesn&#8217;t invalidate the file handle.</simpara>
<programlisting language="zig" linenumbering="unnumbered">Unresolved directive in 28__filesystem-and-io.adoc - include::example$chapters-data/code/28__filesystem-and-io/01_paths_and_Io.zig[]</programlisting>
<formalpara>
<title>Run</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">$ zig run 01_paths_and_io.zig</programlisting>
</para>
</formalpara>
<formalpara>
<title>Output</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">file name: metrics.log
directory: fs_walkthrough
absolute path: /home/zkevm/Documents/github/zigbook-net/fs_walkthrough/metrics.log
--- file contents ---
timestamp,value
2025-11-05T09:00Z,42
2025-11-05T09:05Z,47</programlisting>
</para>
</formalpara>
</section>
<section xml:id="_platform_specific_path_encoding">
<title>Platform-Specific Path Encoding</title>
<simpara>Path strings in Zig use platform-specific encodings, which is important for cross-platform code:</simpara>
<informaltable frame="all" rowsep="1" colsep="1">
<tgroup cols="3">
<colspec colname="col_1" colwidth="33.3333*"/>
<colspec colname="col_2" colwidth="33.3333*"/>
<colspec colname="col_3" colwidth="33.3334*"/>
<thead>
<row>
<entry align="left" valign="top">Platform</entry>
<entry align="left" valign="top">Encoding</entry>
<entry align="left" valign="top">Notes</entry>
</row>
</thead>
<tbody>
<row>
<entry align="left" valign="top"><simpara>Windows</simpara></entry>
<entry align="left" valign="top"><simpara>WTF-8</simpara></entry>
<entry align="left" valign="top"><simpara>Encodes WTF-16LE in UTF-8 compatible format</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>WASI</simpara></entry>
<entry align="left" valign="top"><simpara>UTF-8</simpara></entry>
<entry align="left" valign="top"><simpara>Valid UTF-8 required</simpara></entry>
</row>
<row>
<entry align="left" valign="top"><simpara>Other</simpara></entry>
<entry align="left" valign="top"><simpara>Opaque bytes</simpara></entry>
<entry align="left" valign="top"><simpara>No particular encoding assumed</simpara></entry>
</row>
</tbody>
</tgroup>
</informaltable>
<simpara>On Windows, Zig uses WTF-8 (Wobbly Transformation Format-8) to represent filesystem paths. This is a superset of UTF-8 that can encode unpaired UTF-16 surrogates, allowing Zig to handle any Windows path while still working with <literal>[]const u8</literal> slices. WASI targets enforce strict UTF-8 validation on all paths. On Linux, macOS, and other POSIX systems, paths are treated as opaque byte sequences with no encoding assumptions—they can contain any bytes except null terminators. This means <literal>std.fs.path.join</literal> works identically across platforms by operating on byte slices, while the underlying OS layer handles encoding conversions transparently. When writing cross-platform path manipulation code, stick to <literal>std.fs.path</literal> utilities and avoid assumptions about UTF-8 validity unless targeting WASI specifically.</simpara>
<tip>
<simpara><literal>readToEndAlloc</literal> works on the current seek position; always rewind with <literal>seekTo(0)</literal> (or reopen) after writing if you plan to reread the same handle.</simpara>
</tip>
</section>
</chapter>
<chapter xml:id="streaming-copy">
<title>Streaming copies with positional writers</title>
<simpara>File copying illustrates how <literal>std.fs.File.read</literal> coexists with buffered writers that honor the changelog’s “please buffer” directive. This snippet streams fixed-size chunks, flushes the destination, and fetches metadata for sanity checks.</simpara>
<programlisting language="zig" linenumbering="unnumbered">Unresolved directive in 28__filesystem-and-io.adoc - include::example$chapters-data/code/28__filesystem-and-io/02_stream_copy.zig[]</programlisting>
<formalpara>
<title>Run</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">$ zig run 02_stream_copy.zig</programlisting>
</para>
</formalpara>
<formalpara>
<title>Output</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">copied 17 bytes
destination size: 17
--- copy.txt ---
alpha
beta
gamma</programlisting>
</para>
</formalpara>
<note>
<simpara><literal>File.stat()</literal> caches size and kind information on Linux, macOS, and Windows, saving an extra syscall for subsequent queries. Lean on it instead of juggling separate <literal>fs.path</literal> calls.</simpara>
</note>
</chapter>
<chapter xml:id="dir-walk">
<title>Walking directory trees</title>
<simpara><literal>Dir.walk</literal> hands you a recursive iterator with pre-opened directories, which means you can call <literal>statFile</literal> on the containing handle and avoid reallocating joined paths. The following demo builds a toy log tree, emits directory and file entries, and summarizes how many <literal>.log</literal> files were spotted.</simpara>
<programlisting language="zig" linenumbering="unnumbered">Unresolved directive in 28__filesystem-and-io.adoc - include::example$chapters-data/code/28__filesystem-and-io/03_dir_walk.zig[]</programlisting>
<formalpara>
<title>Run</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">$ zig run 03_dir_walk.zig</programlisting>
</para>
</formalpara>
<formalpara>
<title>Output</title>
<para>
<programlisting language="shell" linenumbering="unnumbered">DIR  logs
DIR  logs/jobs
FILE logs/jobs/batch.log (8 bytes) [log]
DIR  logs/app
FILE logs/app/errors.log (9 bytes) [log]
FILE logs/app/today.log (7 bytes) [log]
DIR  notes
FILE notes/todo.txt (12 bytes)
--- summary ---
directories: 4
files: 4
log files: 3</programlisting>
</para>
</formalpara>
<tip>
<simpara>Each <literal>Walker.Entry</literal> exposes both a zero-terminated <literal>path</literal> and the live <literal>dir</literal> handle. Prefer <literal>statFile</literal> on that handle to dodge <literal>NameTooLong</literal> for deeply nested trees.</simpara>
</tip>
</chapter>
<chapter xml:id="error-patterns">
<title>Error handling patterns</title>
<section xml:id="_how_filesystem_errors_work">
<title>How Filesystem Errors Work</title>
<simpara>The filesystem API returns rich error sets—<literal>error.AccessDenied</literal>, <literal>error.PathAlreadyExists</literal>, <literal>error.NameTooLong</literal>, and friends—but where do these typed errors come from? The following diagram shows the error conversion flow:</simpara>
<literallayout class="monospaced">graph TB
    SYSCALL["System Call"]

    RESULT{"Return Value"}

    subgraph "Error Path"
        ERRNO["Get errno/Win32Error"]
        ERRCONV["Convert to Zig error"]
        RETURN_ERR["Return error"]
    end

    subgraph "Success Path"
        RETURN_OK["Return result"]
    end

    SYSCALL --&gt; RESULT

    RESULT --&gt;|"&lt; 0 or NULL"| ERRNO
    RESULT --&gt;|"&gt;= 0 or valid"| RETURN_OK

    ERRNO --&gt; ERRCONV
    ERRCONV --&gt; RETURN_ERR</literallayout>
<simpara>When a filesystem operation fails, the underlying system call returns an error indicator (negative value on POSIX, <literal>NULL</literal> on Windows). The OS abstraction layer then retrieves the error code—<literal>errno</literal> on POSIX systems or <literal>GetLastError()</literal> on Windows—and converts it to a typed Zig error via conversion functions like <literal>errnoFromSyscall</literal> (Linux) or <literal>unexpectedStatus</literal> (Windows). This means <literal>error.AccessDenied</literal> is not a string or enum tag—it&#8217;s a distinct error type that the compiler tracks through your call stack. The conversion is deterministic: <literal>EACCES</literal> (errno 13 on Linux) always becomes <literal>error.AccessDenied</literal>, and <literal>ERROR_ACCESS_DENIED</literal> (Win32 error 5) maps to the same Zig error, providing cross-platform error semantics.</simpara>
<simpara>Use <literal>catch |err|</literal> sparingly to annotate expected failures (e.g. <literal>catch |err| if (err == error.PathAlreadyExists) {}</literal>) and pair it with <literal>defer</literal> for cleanup so partial successes do not leak directories or file descriptors.</simpara>
</section>
<section xml:id="_the_translation_mechanism">
<title>The Translation Mechanism</title>
<simpara>The error conversion happens through platform-specific functions that map error codes to Zig&#8217;s error types:</simpara>
<literallayout class="monospaced">graph LR
    SYSCALL["System Call&lt;br/&gt;returns error code"]
    ERRNO["errno or NTSTATUS"]
    CONVERT["errnoFromSyscall&lt;br/&gt;or unexpectedStatus"]
    ERROR["Zig Error Union&lt;br/&gt;e.g., error.AccessDenied"]

    SYSCALL --&gt; ERRNO
    ERRNO --&gt; CONVERT
    CONVERT --&gt; ERROR</literallayout>
<simpara>On Linux and POSIX systems, <literal>errnoFromSyscall</literal> in <literal>lib/std/os/linux.zig</literal> performs the errno-to-error mapping. On Windows, <literal>unexpectedStatus</literal> handles the conversion from <literal>NTSTATUS</literal> or Win32 error codes. This abstraction means your error handling code is portable—<literal>catch error.AccessDenied</literal> works identically whether you&#8217;re running on Linux (catching <literal>EACCES</literal>), macOS (catching <literal>EACCES</literal>), or Windows (catching <literal>ERROR_ACCESS_DENIED</literal>). The conversion tables are maintained in the standard library and cover hundreds of error codes, mapping them to approximately 80 distinct Zig errors that cover common failure modes. When an unexpected error occurs, the conversion functions return <literal>error.Unexpected</literal>, which typically indicates a serious bug or unsupported platform state.</simpara>
</section>
<section xml:id="_practical_error_handling_patterns">
<title>Practical Error Handling Patterns</title>
<itemizedlist>
<listitem>
<simpara>When creating throwaway directories (<literal>makePath</literal> + <literal>deleteTree</literal>), wrap deletion in <literal>catch {}</literal> to ignore <literal>FileNotFound</literal> during teardown.</simpara>
</listitem>
<listitem>
<simpara>For user-visible tools, map filesystem errors to actionable messages (e.g. "check permissions on …"). Keep the original <literal>err</literal> for logs.</simpara>
</listitem>
<listitem>
<simpara>If you must fall back from positional to streaming mode, switch to <literal>File.readerStreaming</literal>/<literal>writerStreaming</literal> or reopen in streaming mode once and reuse the interface.</simpara>
</listitem>
</itemizedlist>
</section>
</chapter>
<chapter xml:id="exercises">
<title>Exercises</title>
<itemizedlist>
<listitem>
<simpara>Extend the copy program so the destination filename comes from <literal>std.process.argsAlloc</literal>, then use <literal>std.fs.path.extension</literal> to refuse overwriting <literal>.log</literal> files. <link xl:href="26__build-system-advanced-topics.xml">26</link></simpara>
</listitem>
<listitem>
<simpara>Rewrite the directory walker to emit JSON using <literal>std.json.stringify</literal>, practicing how to stream structured data through buffered writers. See <link xl:href="https://github.com/ziglang/zig/tree/master/lib/std/json.zig">json.zig</link>.</simpara>
</listitem>
<listitem>
<simpara>Build a “tail” utility that follows a file by combining <literal>File.seekTo</literal> with periodic <literal>read</literal> calls; add <literal>--follow</literal> support by retrying on <literal>error.EndOfStream</literal>.</simpara>
</listitem>
</itemizedlist>
</chapter>
<chapter xml:id="notes-caveats">
<title>Notes &amp; Caveats</title>
<itemizedlist>
<listitem>
<simpara><literal>readToEndAlloc</literal> guards against runaway files via its <literal>max_bytes</literal> argument—set it thoughtfully when parsing user-controlled input.</simpara>
</listitem>
<listitem>
<simpara>On Windows, opening directories for iteration requires <literal>OpenOptions{ .iterate = true }</literal>; the sample code does this implicitly via <literal>openDir</literal> with that flag.</simpara>
</listitem>
<listitem>
<simpara>ANSI escape sequences in samples assume a color-capable terminal; wrap prints in <literal>if (std.io.isTty())</literal> when shipping cross-platform tools. See <link xl:href="https://github.com/ziglang/zig/tree/master/lib/std/Io/tty.zig">tty.zig</link>.</simpara>
</listitem>
</itemizedlist>
<section xml:id="_under_the_hood_system_call_dispatch">
<title>Under the Hood: System Call Dispatch</title>
<simpara>For readers curious about how filesystem operations reach the kernel, Zig&#8217;s <literal>std.posix</literal> layer uses a compile-time decision to choose between libc and direct system calls:</simpara>
<literallayout class="monospaced">graph TB
    APP["posix.open(path, flags, mode)"]

    USELIBC{"use_libc?"}

    subgraph "libc Path"
        COPEN["std.c.open()"]
        LIBCOPEN["libc open()"]
    end

    subgraph "Direct Syscall Path (Linux)"
        LINUXOPEN["std.os.linux.open()"]
        SYSCALL["syscall3(.open, ...)"]
        KERNEL["Linux Kernel"]
    end

    ERRCONV["errno → Zig Error"]

    APP --&gt; USELIBC

    USELIBC --&gt;|"true"| COPEN
    USELIBC --&gt;|"false (Linux)"| LINUXOPEN

    COPEN --&gt; LIBCOPEN
    LINUXOPEN --&gt; SYSCALL
    SYSCALL --&gt; KERNEL

    LIBCOPEN --&gt; ERRCONV
    KERNEL --&gt; ERRCONV</literallayout>
<simpara>When <literal>builtin.link_libc</literal> is true, Zig routes filesystem calls through the C standard library&#8217;s functions (<literal>open</literal>, <literal>read</literal>, <literal>write</literal>, etc.). This ensures compatibility with systems where direct syscalls aren&#8217;t available or well-defined. On Linux, when libc is not linked, Zig uses direct system calls via <literal>std.os.linux.syscall3</literal> and friends—this eliminates libc overhead and provides a smaller binary, at the cost of depending on the Linux syscall ABI stability. The decision happens at compile time based on your build configuration, meaning there&#8217;s zero runtime overhead for the dispatch. This architecture is why Zig can produce tiny, static binaries on Linux (no libc dependency) while still supporting traditional libc-based builds for maximum compatibility. When debugging filesystem issues, knowing which path your build uses helps you understand stack traces and performance characteristics.</simpara>
</section>
</chapter>
<chapter xml:id="summary">
<title>Summary</title>
<itemizedlist>
<listitem>
<simpara>Buffer writes, flush deliberately, and lean on <literal>std.fs.File</literal> helpers like <literal>readToEndAlloc</literal> and <literal>stat</literal> to reduce manual bookkeeping.</simpara>
</listitem>
<listitem>
<simpara><literal>Dir.walk</literal> keeps directory handles open so your tooling can operate on basenames without rebuilding absolute paths.</simpara>
</listitem>
<listitem>
<simpara>With solid error handling and cleanup defers, these primitives form the foundation for everything from log shippers to workspace installers.</simpara>
</listitem>
</itemizedlist>
</chapter>
</book>